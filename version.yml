version: 0.11.0.20
default:
    dirtyRepo: dirty-repo
    versionSchema: '{major}.{minor}.{patch}.{build}[-]{branch}[.]{commitShortHash}'
    newBranchSchema: release/v{major}.{minor}.{patch}
    prereleaseTag: local
    precision: Minor
    release:
        match:
            - tag::^v.*
        versionSchema: '{major}.{minor}.{patch}'
branches:      
    branchRelease:
        match:
            - ^release/.*
        versionSchema: '{major}.{minor}.{patch}-{prereleaseTag}[.]{build}}'
        newTagSchema: v{major}.{minor}.{patch}
        precision: Patch
        prereleaseTag: rc
